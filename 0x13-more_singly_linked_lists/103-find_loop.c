#include "lists.h"

/**
 * free_listint_safe - frees a linked list
 * @h: pointer to the firsst node in the linked list
 *
 * Return: number of elements in the freed list
 */
listint_t *find_listint_loop(listint_t *head);
